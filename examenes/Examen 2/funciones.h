#ifndef FUNCIONES_H //hace que el archivo funciones.h solo sea llamado una vez, una vez llamada no volvera a ser llamada
#define FUNCIONES_H
int esPrimo(int a);
int factorial(int a);
int contarDigitos(int a);
#endif
